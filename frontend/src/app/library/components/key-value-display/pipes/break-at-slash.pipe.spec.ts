import { BreakAtSlashPipe } from './break-at-slash.pipe';

describe('BreakAtSlashPipe', () => {
  it('create an instance', () => {
    const pipe = new BreakAtSlashPipe();
    expect(pipe).toBeTruthy();
  });
});
